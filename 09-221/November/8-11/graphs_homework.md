## Библиотеки
```python
import matplotlib.pyplot as plt
import seaborn as sns
import pandas as pd
import numpy as np
````

---

## Задание 1. Анализ продаж магазина

Сгенерируйте данные о продажах магазина за 12 месяцев:

| month | sales |
| ----- | ----- |
| Jan   | 12000 |
| Feb   | 13500 |
| ...   | ...   |

**Задачи:**

1. Постройте линейный график динамики продаж по месяцам.
2. Добавьте сетку, подписи и аннотацию месяца с максимальными продажами.
3. Отобразите тренд (например, `sns.regplot` с `ci=None`).

**Подсказка:**
Используйте `plt.plot()`, `plt.annotate()`, `sns.regplot()`.

---

## Задание 2. Сравнение расходов

Создайте DataFrame с категориями расходов и значениями для трёх человек.

| category      | Alice | Bob | Carol |
| ------------- | ----- | --- | ----- |
| Food          | 300   | 250 | 280   |
| Rent          | 600   | 550 | 700   |
| Transport     | 120   | 150 | 100   |
| Entertainment | 100   | 200 | 150   |

**Задачи:**

1. Постройте группированный столбчатый график.
2. Добавьте легенду, подписи и разные цвета.
3. Сделайте круговую диаграмму расходов Alice.

---

## Задание 3. Температура и осадки

Сгенерируйте данные за 30 дней:

| day  | temp                           | rain |
| ---- | ------------------------------ | ---- |
| 1–30 | случайные температуры и осадки |      |

**Задачи:**

1. Постройте две оси (twinx): температура и осадки.
2. Отобразите разными цветами, добавьте легенду.
3. Добавьте сглаживание температуры (`rolling mean`).

---

## Задание 4. Корреляции экономических показателей

Сгенерируйте таблицу:

| GDP_growth | Unemployment | Inflation | Exports |
| ---------- | ------------ | --------- | ------- |

**Задачи:**

1. Постройте тепловую карту корреляций.
2. Отсортируйте признаки по силе корреляции.
3. Используйте `sns.heatmap()` с `annot=True`, `cmap="coolwarm"`.

---

## Задание 5. Активность пользователей

Создайте DataFrame:

| user_id | age | time_spent | purchases |
| ------- | --- | ---------- | --------- |

**Задачи:**

1. Постройте scatterplot: `age` vs `time_spent`, размер точки — `purchases`.
2. Добавьте прозрачность (`alpha=0.6`), стиль и легенду по размеру.
3. Найдите визуально зависимость между возрастом и активностью.

---

## Задание 6. Сезонность трафика сайта

Сгенерируйте данные по посещаемости сайта за 90 дней.

**Задачи:**

1. Постройте lineplot с выделением выходных другим цветом.
2. Добавьте вертикальные линии для понедельников.
3. Сгладьте кривую с помощью `rolling(7).mean()`.

---

## Задание 7. Распределение доходов

Сгенерируйте 500 случайных значений дохода (`np.random.lognormal`).

**Задачи:**

1. Постройте гистограмму и kdeplot.
2. Добавьте линии медианы и 90-го перцентиля.
3. Используйте кастомную палитру и подписи.

---

## Задание 8. Задержки авиарейсов

Загрузите CSV с данными о рейсах (например, `flights.csv` с Kaggle).

**Задачи:**

1. Постройте распределение задержек вылетов (`departure_delay`).
2. Boxplot по авиакомпаниям.
3. Найдите компанию с минимальной медианной задержкой.

---

## Задание 9. Соотношение признаков на реальном датасете

Возьмите любой CSV-файл (продажи, спорт, демография и т.д.).

**Задачи:**

1. Постройте `pairplot` для числовых признаков.
2. Добавьте цветовую метку по категории.
3. Найдите пары признаков с визуальной корреляцией.

---

## Задание 10. Мини-проект: История в графиках

Выберите собственные или открытые данные и создайте 3–4 визуализации.

**Требования:**

* минимум 3 графика (разных типов);
* оформление (заголовки, легенды, цвета);
* краткое описание наблюдений.
